# .obstral.md — Runtime eval fixture

## Stack
Git + text

## Test Command
test_cmd: test -d demo_repo/.git && test -f demo_repo/README.md && test -f demo_repo/.gitignore

## Development Rules
- Create the requested repo artifact under tool_root
- Verify the repo directory and starter files before calling done
