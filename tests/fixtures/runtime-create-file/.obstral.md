# .obstral.md — Runtime eval fixture

## Stack
Text

## Test Command
test_cmd: test -f notes/todo.txt && grep -Fx "ship it" notes/todo.txt

## Development Rules
- Create the requested artifact with the smallest safe change
- Verify the requested file exists before calling done
