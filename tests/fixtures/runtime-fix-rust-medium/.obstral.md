# .obstral.md — Runtime eval fixture

## Stack
Rust

## Test Command
test_cmd: cargo test 2>&1

## Development Rules
- Read the target file before editing it
- Make the smallest code change that fixes the failing test
- Run tests before calling done
